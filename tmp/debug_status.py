import json
import base64

# Simulating the fetch via a system-level tool because Prisma is hard to reach via standalone TS on this env
# Actually, I'll just use the `find_dir` and `grep` to find if the user exists in any local json if relevant
# But I can just check the API response properly.
